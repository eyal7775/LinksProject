pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script {
                    env.HELLO = sh(script: "python hello.py, returnStdout: true).toString().trim()
                }
            }
        }
    }
}

// press F1 find Validate Jenkinsfile
// http://localhost:8080/pipeline-model-converter/validate